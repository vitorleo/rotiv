# @rotiv/* packages are optional — install will succeed even if they are not yet on npm.
# Once @rotiv/* packages are published, remove this file and run pnpm install again.
optional=true

# Allow pnpm v10 to build native addons without interactive approval.
onlyBuiltDependencies[]=better-sqlite3
onlyBuiltDependencies[]=esbuild
